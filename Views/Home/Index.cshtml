@model TipCalculatorModel
@{
    ViewData["Title"] = "Tip Calculator";
    Layout = "_LayoutCalculator";
}

<h1>Tip Calculator</h1>
<form asp-action="Index" method="post">
    <div asp-validation-summary = "All"></div>
    <div>
        <label asp-for="BillAmount">BillAmount:</label>
        <input asp-for="BillAmount" />
    </div>
    <div>
        <label>15% tip:</label>
        <span>@ViewBag.Tip15.ToString("C2")</span>
    </div>
    <div>
        <label>20% tip:</label>
        <span>@ViewBag.Tip20.ToString("C2")</span>
    </div>
    <div>
        <label>25% tip:</label>
        <span>@ViewBag.Tip25.ToString("C2")</span>
    </div>
    <button type="submit">Calculate</button>
    <a asp-action="Index">Clear</a>
</form>